<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_search"
        android:title="@string/meklet"
        android:icon="@drawable/ic_search"
        android:iconTint="@color/white"
        app:showAsAction="ifRoom"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/menu_sortBy"
            android:title="@string/kartot_pec">
            <menu>
                <item
                    android:id="@+id/menu_priority_high"
                    android:title="@string/augsta_pioritate"/>
                <item
                    android:id="@+id/menu_priority_low"
                    android:title="@string/zema_pioritate"/>
            </menu>
        </item>
    </group>

    <item
        android:id="@+id/menu_delete_all"
        android:title="@string/Izdzest_visus"
        app:showAsAction="never"/>

</menu>